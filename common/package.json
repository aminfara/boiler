{
  "name": "@boiler/common",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "tsx --watch src/index.ts",
    "build:check": "tsc --build tsconfig.build.json",
    "build:emit": "tsx ./bundle.ts",
    "build": "npm run build:check && npm run build:emit",
    "start": "node dist/index.js"
  }
}
