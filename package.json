{
  "name": "@boiler/root",
  "version": "0.0.1",
  "description": "A typescript fullstack boilerplate.",
  "license": "MIT",
  "workspaces": [
    "api",
    "app"
  ],
  "scripts": {
    "clean": "npm -w api run clean && npm -w app run clean",
    "build": "tsc --build --verbose"
  },
  "devDependencies": {
    "@types/node": "^18.18.6",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
